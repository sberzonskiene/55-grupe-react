export function Calc ({ a, b }) {
    //const a = props.a;
    //const b = props.b; arba
    //const  = props; arba rasyti tiesias i funkcijos ()
    
    return(
        <p className="calculator">{a} + {b} = {a + b}</p>
    );
}